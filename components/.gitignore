# Dependencies (たくさんの部品が入っているフォルダなので無視)
/node_modules
/.pnp
.pnp.js

# Testing (テスト結果など)
/coverage

# Next.js build output (Next.jsが自動で作るファイル)
/.next/
/out/

# Production build files
# /dist

# ESLint cache
.eslintcache

# Temporary files (一時ファイル)
*.swp
*~
*.bak

# Local environment variables (秘密の情報を書くファイルなので無視)
# これらは後でVercelに直接教えます
.env*.local
.env

# Optional VS Code files (VSCodeの設定ファイルなど、好みで)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Mac files (Macが自動で作るファイル)
.DS_Store

# Log files (ログファイル)
npm-debug.log*
yarn-debug.log*
yarn-error.log*